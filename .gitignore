# 비밀키 — 절대 커밋하지 않음
.env
.env (2).example

# 원본 API 응답 캐시 (용량 크고, 재실행하면 다시 만들어짐 — 배포에도 필요 없음)
data/raw/

# data/processed 중 배포된 앱이 실제로 읽는 건 analysis_dataset.parquet 하나뿐이라
# 나머지 중간 산출물은 제외 (필요하면 로컬 파이프라인 재실행으로 다시 만들 수 있음)
data/processed/*
!data/processed/analysis_dataset.parquet

# 파이썬 캐시
__pycache__/
*.pyc
.venv/
venv/

# OS/에디터
.DS_Store
.vscode/
